import React from "react";
import { useStyles } from "../Styles/CustomStyles";

const Search = (props) => {
    const classes = useStyles();

    return <div className={classes.pageContent}>This is Search Page</div>;
};

export default Search;
